# Contributing

`xndbc` is still in development. If you want to contribute, open an issue or pull request on the project repository.

For local development, create the development environment and run the test suite:

```bash
conda env create -f docs/environment.yml
conda activate xndbc-dev
pytest
```
